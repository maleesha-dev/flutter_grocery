import 'package:flutter/material.dart';

class PriceListCard extends StatelessWidget {
  const PriceListCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
